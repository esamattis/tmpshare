<!doctype html>
<html>
    <head>
        <meta charset="utf-8">
        <title></title>
    </head>
    <body>

        <p>
            You uploaded multiple files. Here's preview link for each
        </p>

        <% previewURLs.forEach(function(url) { %>

            <p>
                <a href="<%= url %>"><%= url %></a>
            </p>

        <% }) %>

    </body>
</html>
